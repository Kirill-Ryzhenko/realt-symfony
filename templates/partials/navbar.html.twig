<nav>
  <div class="nav-wrapper">
    <a href="{{ path('index') }}" class="brand-logo">{{ 'website.navbar.logo'|trans }}</a>
    <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
    <ul class="right hide-on-med-and-down">
      <li><a href="{{ path('announcement_filter', {'type': 'sale'}) }}">Продажа</a></li>
      <li><a href="{{ path('announcement_filter', {'type': 'rent'}) }}">Аренда</a></li>
      {% if app.user %}
        <a class="dropdown-trigger btn" href="#" data-target="dropdown1" style="text-transform: lowercase">
          {{ app.user.email }}
        </a>
        <ul id="dropdown1" class="dropdown-content">
          <li><a href="{{ path('profile', {'id': app.user.id}) }}">Профиль</a></li>
          <li><a href="{{ path('announcement_new', {'type': 'sale'}) }}">Продать квартиру</a></li>
          <li><a href="{{ path('announcement_new', {'type': 'rent'}) }}">Сдать в аренду квартиру</a></li>
          <li><a href="{{ path('support_message') }}">Написать в поддержку</a></li>
          <li class="divider" tabindex="-1"></li>
          <li><a href="{{ path('app_logout') }}">Выйти</a></li>
        </ul>
      {% else %}
        <li><a href="{{ path('app_login') }}">Вход</a></li>
        <li><a href="{{ path('registration') }}">Регистрация</a></li>
      {% endif %}
    </ul>
  </div>
</nav>

<ul class="sidenav" id="mobile-demo">
  <li><a href="{{ path('announcement_filter', {'type': 'sale'}) }}">Продажа</a></li>
  <li><a href="{{ path('announcement_filter', {'type': 'rent'}) }}">Аренда</a></li>
  {% if app.user %}
    <li><a href="{{ path('profile', {'id': app.user.id}) }}">Профиль</a></li>
    <li><a href="{{ path('announcement_new', {'type': 'sale'}) }}">Продать квартиру</a></li>
    <li><a href="{{ path('announcement_new', {'type': 'rent'}) }}">Сдать в аренду квартиру</a></li>
    <li><a href="{{ path('support_message') }}">Написать в поддержку</a></li>
    <li class="divider" tabindex="-1"></li>
    <li><a href="{{ path('app_logout') }}">Выйти</a></li>
  {% else %}
    <li class="divider" tabindex="-1"></li>
    <li><a href="{{ path('app_login') }}">Вход</a></li>
    <li><a href="{{ path('registration') }}">Регистрация</a></li>
  {% endif %}
</ul>

